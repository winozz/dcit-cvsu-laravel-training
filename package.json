{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "ws": "php artisan websockets:serve",
        "soketi": "npx @soketi/soketi@latest start --host 0.0.0.0 --port 6001 --app-id local-ws --key localkey --secret localsecret --debug"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    },
    "dependencies": {
        "laravel-echo": "^2.3.1",
        "pusher-js": "^8.4.0"
    }
}
